```@meta
CurrentModule = PlasticRecurrentNeuralNetworks
```

# Topology Utilities

These helpers generate simple population layouts and connection scaling matrices
using the package convention `post <- pre`.

```@autodocs
Modules = [PlasticRecurrentNeuralNetworks]
Pages = ["topology_utility_functions.jl"]
```
